const colors = require("colors");
const moment = require("moment");

console.log(moment().format());
console.log(colors.green("Application starting..."));
// console.log('Application starting....'.bgRed);