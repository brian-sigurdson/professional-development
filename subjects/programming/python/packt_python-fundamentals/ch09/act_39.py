import string

# key error
# building = dict(name="xyz towers", type="business premises")
# print(building['missing'])

# attribute error
name = "brian"
print(name.lower())
print(name.size())
